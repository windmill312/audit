# Site visiting monitoring service

## Run
You can run service with `docker-compose up` command. 
After that you can see OpenAPI via browser on http://localhost:8101/swagger-ui.html

## Load testing
Load testing implemented with gatling framework and can be started by:
`mvn gatling:test`